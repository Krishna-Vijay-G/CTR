import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.chennaiturboriders.racing',
  appName: 'chennai-turbo-riders',
  webDir: 'out'
};

export default config;
